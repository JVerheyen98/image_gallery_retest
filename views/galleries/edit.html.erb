<h1>Editing gallery</h1>

<%= render "form", gallery: @gallery %>

<br>

<div>
  <%= link_to "Show this photo", gallery_photo_path(@gallery, @photo) %> |
  <%= link_to "Back to gallery", gallery_path(@gallery) %>
</div>
